apiVersion: v1
kind: Secret
metadata:
  name: cockroachdb-minio-creds
  namespace: cockroachdb-test
type: Opaque
stringData:
  rootUser: minioadmin
  rootPassword: minioadmin123
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cockroachdb-minio
  namespace: cockroachdb-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cockroachdb-minio
  namespace: cockroachdb-test
  labels:
    app: cockroachdb-minio
    cockroachdb-minio: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cockroachdb-minio
  template:
    metadata:
      labels:
        app: cockroachdb-minio
    spec:
      containers:
        - name: minio
          image: minio/minio:RELEASE.2025-04-22T22-12-26Z
          args:
            - server
            - /data
            - --console-address
            - ":9001"
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: cockroachdb-minio-creds
                  key: rootUser
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cockroachdb-minio-creds
                  key: rootPassword
          ports:
            - containerPort: 9000
              name: api
            - containerPort: 9001
              name: console
          volumeMounts:
            - name: data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /minio/health/ready
              port: 9000
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: cockroachdb-minio
---
apiVersion: v1
kind: Service
metadata:
  name: cockroachdb-minio
  namespace: cockroachdb-test
spec:
  selector:
    app: cockroachdb-minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
