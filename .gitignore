.DS_Store
feature-requests/
# MS Office lock / owner files (created while a doc is open)
~$*
# Internal brown-bag-lunch deck — not part of any blueprint
bbl.pptx
# cpd-cli scratch (log dir created wherever cpd-cli is invoked)
cpd-cli-workspace/
