apiVersion: v1
kind: Secret
metadata:
  name: pg-cluster-minio-creds
  namespace: cnpg-barman
type: Opaque
stringData:
  rootUser: minioadmin
  rootPassword: minioadmin123
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-cluster-minio
  namespace: cnpg-barman
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pg-cluster-minio
  namespace: cnpg-barman
  labels:
    app: pg-cluster-minio
    cnpg-barman-minio: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pg-cluster-minio
  template:
    metadata:
      labels:
        app: pg-cluster-minio
    spec:
      containers:
        - name: minio
          image: minio/minio:RELEASE.2025-04-22T22-12-26Z
          args:
            - server
            - /data
            - --console-address
            - ":9001"
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: pg-cluster-minio-creds
                  key: rootUser
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-cluster-minio-creds
                  key: rootPassword
          ports:
            - containerPort: 9000
              name: api
            - containerPort: 9001
              name: console
          volumeMounts:
            - name: data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /minio/health/ready
              port: 9000
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pg-cluster-minio
---
apiVersion: v1
kind: Service
metadata:
  name: pg-cluster-minio
  namespace: cnpg-barman
spec:
  selector:
    app: pg-cluster-minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
